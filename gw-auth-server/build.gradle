apply from: '../gradle/docker.gradle'

dependencies {
    compile("org.springframework.cloud:spring-cloud-starter-netflix-zuul:1.4.0.RELEASE")

    // for OAuth2 & TokenStore
    compile('org.springframework:spring-jdbc')
    compile('com.h2database:h2')
}